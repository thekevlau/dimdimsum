import { Flummox } from 'flummox';

export default class Flux extends Flummox {
    constructor(){
        super();

        // Create actions and stores here.
        // Sample:
        // this.createActions('UiActions', UiActions);
        // this.createStore('UiStore', UiStore, this);
    }
};
